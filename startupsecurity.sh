#!/bin/bash

#On startup this script checks the startup folder for a password script,
#runs the script and ask the user for a secondary password before shutting down the computer within 'n' seconds.

if [[ $
